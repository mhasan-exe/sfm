# TODO_PROGRESS

- [x] Inspect existing AdminLogsPage and AuditLogService.
- [x] Implement AuditLogService pagination (`fetchAuditLogsPage`).

- [ ] Rebuild AdminLogsPage with explicit “Load more” and local state (no auto/stream infinite).
- [ ] Ensure UI uses a totally different approach than StreamBuilder/ListView.
- [ ] Run `flutter analyze` and basic compile check.

